# 工作流 Staging 演练记录（2026-02-12）

## 1. 演练基本信息

- 演练日期：2026-02-12
- 演练环境：staging（预检查链路，本地等价执行）
- 演练负责人：待补充
- 参与人：待补充
- 目标版本（commit/tag）：待补充
- 演练窗口（开始~结束）：16:35 ~ 16:36（UTC+0 见报告 generatedAt）

## 2. 演练目标

1. 验证 workflow 主链路（定义/发布/执行/时间线/审计）可用。
2. 验证权限边界（触发人/owner/outsider）符合预期。
3. 验证质量门禁与基线报告可稳定输出。
4. 验证回滚流程可在规定时间内完成。

## 3. 演练前准备检查

1. staging 数据库可用，迁移状态正常：已通过（等价环境）。
2. staging 已部署待演练版本：待补充。
3. 值班沟通群已建立并明确回滚负责人：待补充。
4. 历史稳定版本可快速切换：待补充。

## 4. 预检查命令执行记录

首次执行：

```bash
pnpm workflow:drill:staging:precheck
```

结果：失败（非业务失败）
- 原因：`turbo` 本机 TLS/keychain 异常（`No keychain is available`）。

等价拆分执行（成功）：

```bash
pnpm --filter @packages/types type-check
pnpm --filter api type-check
pnpm --filter web type-check
pnpm workflow:smoke:gate
pnpm --filter api run workflow:execution:baseline -- --thresholds-file=../../config/workflow-execution-baseline-thresholds.json --use-default-gate-thresholds --days=7 --report-file=../../logs/workflow-execution-baseline-staging-drill.json
pnpm workflow:execution:baseline:report:validate -- --report-file=logs/workflow-execution-baseline-staging-drill.json --summary-json-file=logs/workflow-execution-baseline-staging-drill-validation.json --require-gate-pass --require-gate-evaluated
pnpm workflow:execution:baseline:reference -- --mode=ensure --current-report=logs/workflow-execution-baseline-staging-drill.json --reference-report=logs/workflow-execution-baseline-reference.json --summary-json-file=logs/workflow-execution-baseline-reference-operation.json
pnpm workflow:execution:baseline:trend -- --current-report=logs/workflow-execution-baseline-staging-drill.json --reference-report=logs/workflow-execution-baseline-reference.json --summary-json-file=logs/workflow-execution-baseline-staging-trend.json --require-reference
pnpm workflow:execution:baseline:reference -- --mode=promote --current-report=logs/workflow-execution-baseline-staging-drill.json --reference-report=logs/workflow-execution-baseline-reference.json --summary-json-file=logs/workflow-execution-baseline-reference-operation.json
pnpm workflow:drill:staging:precheck:summary
```

补充复验（2026-02-13）：

```bash
pnpm workflow:drill:staging:precheck:fast
```

结果：成功（`status=SUCCESS`，报告时间 `2026-02-13T01:13:36.637Z`）。

提速入口（后续批量演练建议）：

```bash
pnpm workflow:drill:staging:full
```

整链复验（2026-02-13）：

```bash
pnpm workflow:drill:staging:full
```

结果：成功（`precheck:fast` 与 `rollback:verify` 均通过，产物已落盘到 `logs/*`）。

closeout 收口（2026-02-13）：

```bash
pnpm workflow:drill:staging:closeout
```

结果：成功（`status=SUCCESS`，`releaseDecision=READY_TO_PROMOTE`，生成时间 `2026-02-13T02:39:48.862Z`）。
说明：当前仅缺 CI run 链接/结论，closeout 以 warning 提示，未阻断本地演练结论。

## 5. 演练步骤记录

### 5.1 功能链路演练

- [x] 定义创建/版本发布成功
- [x] 发布审计查询正确
- [x] 执行触发成功
- [x] timeline 查询与筛选正确
- [x] 失败分类筛选正确（TIMEOUT/CANCELED/EXECUTOR）

说明：通过 `pnpm --filter api run test:e2e:workflow-p1-core` 与 `pnpm --filter api run test:e2e:workflow-p2-reliability` 完成等价功能链路验收。

### 5.2 权限边界演练

- [x] outsider 不可取消 owner 执行
- [x] outsider 不可重跑 owner 失败实例
- [x] outsider 不可查看 owner 执行详情/时间线
- [x] owner 可查看并管理自身流程执行

说明：已由 `workflow-p2-reliability.e2e` 覆盖并通过。

### 5.3 指标与门禁演练

- [x] `workflow:execution:baseline:gate` 产出报告
- [x] `workflow:execution:baseline:report:validate` 产出 validation 报告
- [x] `workflow:execution:baseline:reference -- --mode=ensure/promote` 产出 reference operation 报告
- [x] `workflow:execution:baseline:trend` 产出趋势对比报告
- [x] 报告字段完整（rates/latency/gate）
- [x] Step Summary 本地联动核验（`workflow:ci:step-summary` 已读取 baseline 报告并渲染区块）
- [x] Step Summary 区块完整性本地校验（`workflow:ci:step-summary:validate` 通过，`missingSections=0`）
- [x] `workflow:drill:staging:closeout` 生成收口报告（`status=SUCCESS`，`releaseDecision=READY_TO_PROMOTE`）
- [ ] CI Step Summary 区块联动核验（待在 CI 环境确认）
- [ ] CI run 链接与结论回填（执行严格 closeout：`--require-ci-run-url --require-ci-run-success`）

关键产物：
- `logs/workflow-execution-baseline-staging-drill.json`
- `logs/workflow-execution-baseline-staging-drill-validation.json`
- `logs/workflow-execution-baseline-reference-operation.json`
- `logs/workflow-execution-baseline-staging-trend.json`
- `logs/workflow-drill-staging-precheck-summary.md`
- `logs/workflow-drill-staging-precheck-summary.json`
- `logs/workflow-drill-staging-full-summary.md`
- `logs/workflow-drill-staging-full-summary.json`
- `logs/workflow-drill-staging-closeout.md`
- `logs/workflow-drill-staging-closeout.json`

本次 precheck 摘要：
- 状态：`SUCCESS`
- 生成时间：`2026-02-13T02:38:59.955Z`
- Validation Error Count：`0`
- Warning Count：`0`
- 样本量：`executions=0`（baseline gate 通过，且输出空样本告警）

## 6. 回滚演练记录（必填）

- 回滚开始时间：2026-02-13 09:15（本地时间 UTC+8）
- 回滚完成时间：2026-02-13 09:16（本地时间 UTC+8）
- RTO（分钟）：约 1 分钟
- 回滚后 smoke：通过（`logs/workflow-smoke-gate-report.json`，`status=SUCCESS`）
- 回滚后 baseline 报告：`logs/workflow-execution-baseline-post-rollback.json`
- 回滚后 baseline 校验：`logs/workflow-execution-baseline-post-rollback-validation.json`（`status=SUCCESS`）
- 回滚后 trend 报告：`logs/workflow-execution-baseline-post-rollback-trend.json`（`status=SUCCESS`）
- 回滚后 successRate：`0`（样本量 0）
- 回滚后 failedRate：`0`（样本量 0）
- 回滚后 timeoutRate：`0`（样本量 0）
- 回滚结论：通过（流程链路可执行，基线/趋势校验通过；需在 CI 环境补一轮联动确认）

## 7. 问题与改进项

1. 问题：`workflow:drill:staging:precheck` 在本机被 `turbo` keychain/TLS 异常阻断。  
   - 影响范围：本地一键预检命令可用性。  
   - 根因判断：环境级 keychain 不可用，不属于 workflow 业务逻辑错误。  
   - 临时措施：采用等价拆分命令执行预检链路。  
   - 修复动作（本次已完成）：新增 `pnpm workflow:drill:staging:precheck:fast`（分包 type-check 快通道），并同步模板/SOP。  
   - 负责人：待补充  
   - 计划完成日期：已完成（2026-02-13）

2. 问题：一键链路中 baseline 报告路径在 root/api 间出现偏差。  
   - 影响范围：`baseline:report:validate` 读取失败。  
   - 根因判断：嵌套 `pnpm run` 参数透传导致 `--report-file` 未按预期生效。  
   - 修复动作（本次已完成）：  
     - `apps/api/package.json` 中 `workflow:execution:baseline:gate` 改为直接 `ts-node` 调用，移除双层 `pnpm run`。  
     - `package.json` 的 `workflow:drill:staging:precheck`、`Workflow Smoke` CI、`工作流Staging演练记录模板` 的 baseline gate 命令统一改为 `--report-file=../../logs/...`。  
   - 复验结果：precheck 拆分链路已成功通过并生成 root `logs/*` 产物。  
   - 负责人：待补充  
   - 计划完成日期：已完成（2026-02-12）

## 8. 演练结论

- 是否满足上线条件：基本满足（功能链路/权限链路、precheck、回滚实战均完成；待补 CI 环境联动确认）
- closeout 当前结论：`READY_TO_PROMOTE`（本地等价环境），但 CI 证据尚未回填。
- 阻塞项：
  1. CI Step Summary 区块联动尚未在 staging CI 实跑确认。
  2. CI run URL/结论尚未补录到 closeout（严格模式未执行）。
- 下次复演计划日期：待补充
